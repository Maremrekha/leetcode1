from typing import List


class Solution:
    def largest(self, arr):
        # code here
        maxi=0
        for i in range(0,len(arr)):
            if(arr[i]>maxi):
                maxi=arr[i]
        return maxi
